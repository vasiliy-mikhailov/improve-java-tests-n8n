# secrets — never commit
.env
.env.production
.env.server
.env.local

# recovered prior-build reference (not part of the project)
reference/

# local artifacts
node_modules/
*.log
.DS_Store
.env.deploy
.claude/

# fixtures are recorded model answers; refresh with MODEL_TEST_REFRESH=1

# mutation-testing scratch and reports (regenerated by `npx stryker run`)
.stryker-tmp/
reports/

# Maven build output for the Java backend. Committing target/ once cost this repo a
# 133 MB blob in history and a rejected push; the jar is a build artefact, not a source.
backend/target/
