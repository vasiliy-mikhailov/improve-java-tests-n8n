{
 "note": "DoD self-assessment per RESEARCH.md §2 (14 items). Each ∈ {0, 0.5, 1}. Scored on OBSERVED evidence only — an item stays at 0.5 until it has been seen working on real repos, not just on the synthetic one.",
 "assessed": "2026-07-26, iteration 4",
 "items": {
  "D1_one_command_docker": 1,
  "D2_repo_is_configuration": 1,
  "D3_metrics_per_file_and_overall": 1,
  "D4_tests_actually_improve": 1,
  "D5_pr_per_improved_file": 1,
  "D6_live_stage_visibility": 1,
  "D7_per_stage_rules": 1,
  "D8_n8n_native_only": 1,
  "D9_adaptable_across_java_builds": 0.5,
  "D10_protected_access": 1,
  "D11_no_reasoning_leakage": 1,
  "D12_no_dead_weight_tests": 0.5,
  "D13_human_equivalent_timesheets": 1,
  "D14_llm_token_accounting": 1
 },
 "justification": {
  "D1": "docker compose up -d --build brings up n8n + sidecar + dashboard in one container (Temurin 8/11/17/21, Maven 3.9.9, Gradle 8.10.2, gh); the ijt_data volume survives restarts; entrypoint automates owner setup, workflow import + activation and the 10-year token mint.",
  "D2": "Synth (file:// remote) plus ten GitHub repos targeted purely through env / webhook config — Maven and Gradle, single and multi-module, JUnit 4 and 5, no source edits.",
  "D3": "Per-class JaCoCo line coverage, PIT mutation score and MAC, before and after, stored per file and exposed via /api/metrics and the dashboard. Parsers pinned by unit tests against realistic report fragments after two parsing defects (method-counter, self-closing class) produced wrong per-class numbers.",
  "D4": "Observed ΔMAC > 0 with the suite green: synth Pricing 0→100, synth Validator 8.51→…, commons-text Counter 0→100 (100 % covered, 0 % mutation — the archetype), json-java XMLParserConfiguration 0→14.65. Multi-round criterion and the diminishing-returns stop both observed firing.",
  "D5": "One branch + cumulative PR per improved class, none for unimproved ones; prepared-PR artifacts under /data/prs in local mode. Verified on synth and on json-java / commons-text.",
  "D6": "Stage and live progress line update within ~2 s on the dashboard; every stage of the brief (picking a class, improving coverage, improving mutation, improving MAC, preparing PR) observed live during runs.",
  "D7": "All six stages applied. Observed: UI classes excluded from picking by rule, branch-per-class naming, PR titled and structured per AGENTS.md, check_changes vetoing a non-improving change ('MAC did not improve (0 → 0)').",
  "D8": "65 nodes, all native (trigger / HTTP Request / Code / IF / NoOp). Generator refuses to emit if a non-native node appears, if a Code node references child_process/fs, or if any Code node fails to parse.",
  "D9": "0.5 — Maven is proven across five repos (single and multi-module, JDK fallback discovering the real build JDK, JaCoCo agent conflict handled, PIT wiring injected for JUnit 5). The GRADLE path has produced no improvement yet: the PIT init script was rejected for an insecure http repository, and after that fix json-path still produced no report. Two Gradle repos remain unproven.",
  "D10": "Caddy basic auth in front of improve-java-tests-n8n.mikhailov.tech with its own password; 10-year n8n JWT injected via header_up Cookie so n8n never shows a login screen; dashboard at /dashboard. Verified: 401 without credentials, 200 with, /rest/login returns the owner account.",
  "D11": "Model thinking runs in the thinking channel and a cleanup pass strips residual scratch commentary; PR'd tests inspected (ValidatorMacCovTest) contain no chain-of-thought, only assertions.",
  "D12": "0.5 — the verified cleanup pass exists and re-measures before keeping (revert on green-suite failure or a mutation-score drop), and generated tests inspected so far assert real values. But it has not yet been observed pruning a vacuous test on a real repo, so this is credited as partial.",
  "D13": "Itemized per-class estimate (analysis / test writing / mutation analysis / verification) with machine time, ETA, FTE and progress on the dashboard. Test-case counting was JS-specific and reported 0 for every Java diff until fixed to count @Test / @ParameterizedTest / @RepeatedTest annotations.",
  "D14": "Verified live on JSON-java: 3 LLM calls, 8874 input / 12471 output tokens, attributed to the unit being improved (org/json/Cookie.java::<init>). Usage is read from the endpoint's `usage` field on every completion including retries and repair passes, and shown per unit and per run with tokens-per-improved-unit alongside machine time and human-equivalent hours.",
  "D3_metrics_per_file_and_overall": "Held at 1, but every number it reports was wrong today and is now right: PIT reports are scoped to the unit's own method before scoring (JSONObject#isRecordStyleAccessor read 15.79% over toString's mutants; it is 0%), PIT method names are XML-decoded so constructors are no longer measured as having no mutation surface, JaCoCo exec data is cleared before every run (a deleted test's coverage had been counted for three runs, and the repo headline read 91.4% where it is 90%), and units with no mutation surface are excluded from the headline averages.",
  "D9_adaptable_across_java_builds": "0.5. The Gradle wiring is now under unit test — the plugin-version rule that ended every Gradle run (1.15.0 reads reporting.baseDir, removed in Gradle 9), method scoping, the JUnit 5 branch, and the http Nexus mirror Gradle 7+ refuses without allowInsecureProtocol. That removes the known mechanical failures but proves nothing: no Gradle repo has been run end to end. concurrency-limits and java-dataloader are queued for it.",
  "D12_no_dead_weight_tests": "0.5. Its blocker is fixed — the plausibility gate tested for `it(`/`describe(`, vitest shapes no Java file has, so the cleanup pass had never once run on a Java repo — and it now gates on Java questions (class name preserved, no @Test invented compared BY NAME, no reflection smuggled in, not truncated). Dead weight is also pruned before a PR: a zero-byte generated test reached a prepared patch, and the root cause is fixed too (`git add -N` for the dashboard diff made the file tracked, so a missed round's `git clean` left it behind as an empty blob). Still 0.5 because the verified-cleanup path — strip, re-measure, revert if the mutation score drops — has not yet been observed doing so on a real file; the one PR so far needed no stripping.",
  "D5_pr_per_improved_file": "1. Re-verified on a from-scratch run: JSONArray#getNumber, coverage 0>100, mutation 0>66.67, MAC 0>66.67, one prepared PR with two real test files and a metrics table. The body no longer claims compatibility the run never measured (\"Tests pass on Java 1.6 - 25\" for work verified on one JDK)."
 }
}